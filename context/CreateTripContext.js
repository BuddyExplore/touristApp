import { createContext } from "react";

export const CreateTripContext=createContext(null);

// import React, { createContext } from 'react';

// const CreateTripContext = createContext(null);

// export default CreateTripContext;